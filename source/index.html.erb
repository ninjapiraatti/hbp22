---
title: HBP
---
<link rel="stylesheet" href="stylesheets/all.css">
<header class='nav-bg-blue'>
<% data.stuff.header.each do |id, header| %>
  <nav>
    <ul id="main-nav" class="nav">
      <li>
        <a href='/'>
          <div class="text-white">
          <img src='<%= header["logo"]["url"] %>'/>
            <%= header["title"] %>
          </div>
        </a>
      </li>
      <li>
          <a class="nav-link text-white" href="#info"><%= header["info"] %></a>
      </li>
      <li>
          <a class="nav-link text-white" href="#gallery"><%= header["moments"] %></a>
      </li>
      <li>
          <a class="nav-link text-white" href="#yhteistyokumppanit"><%= header["moments"] %></a>
      </li>
      <li>
          <a class='nav-link text-white language-link' href='/en/'><%= header["english"] %></a>
      </li>
      <li>
          <a class='nav-link text-white language-link' href='/fi/'><%= header["finnish"] %></a>
      </li>
    </ul>
  </nav>
<% end %>
</header>

<div class="jumbotron">
  <div class="container">

    <div class="md-col-8 md-offset-2">
        <% data.stuff.header.each do |id, header| %>
          <h1 class="black"><%= header["title"] %></h1>
        <% end %>
    </div>
  </div>

  <div class="md-col-8 md-offset-2">
    <% data.stuff.body.each do |id, body| %>
      <p class="thin"><%= body["text_body"] %></p>
    <% end %>
  </div>

  <footer>
    <% data.stuff.footer.each do |id, footer| %>
      <p class="lightItalic"><%= footer["copyright"] %></p>
    <% end %>
  </footer>

</div>
<link rel='stylesheet' href='https://cdn.jsdelivr.net/npm/bootstrap@latest/dist/css/bootstrap.min.css'>
<script src='https://cdn.jsdelivr.net/npm/bootstrap@latest/dist/js/bootstrap.bundle.min.js'></script>